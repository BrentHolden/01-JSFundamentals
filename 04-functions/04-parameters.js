/*function pet(animal) {
    console.log(`I have a/n ${animal} for a pet.`);
}

pet("cat");*/

let firstName = 'Brent';
let lastName = 'Holden';
let wholeName = `${firstName} ${lastName}`

function fullName(wholeName){
    console.log(`My full name is ${wholeName}.`)
}

fullName(wholeName);

/*let firstName = 'Brent';
let lastName = 'Holden';

function fullName(firstName, lastName){
    let wholeName = firstName + ' ' + lastName;
    console.log(`My full name is ${wholeName}.`)
}

fullName(firstName, lastName);*/